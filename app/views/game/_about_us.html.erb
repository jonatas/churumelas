<div class="about-us">
<% %w(jonatasdp eliegejachini).each do |who| %>
    <div class="<%= who %>">
      <%= image_tag "about-#{who}.png" %>
      <div class="connections">
        <% %w(google github instagram twitter).each do |connection|
          username = override_username[who] && override_username[who][connection] || who
         link_url = 
          if override_url[connection]
            override_url[connection].call connection, username
          else
            "http://#{connection}.com/#{username}"
          end
      %> 
      <%= link_to image_tag("#{connection}.png"),link_url, :target => "_blank" %>
        <% end %>
      </div>
    </div>
  <% end %>
<a href="http://railsrumble.com/entries/370-churumelas-small-challenges", class="rumble" target="_blank"><%= image_tag("rails-rumble.png")   %></a>
</div>
